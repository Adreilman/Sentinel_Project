# Sentinel Dashboard Deployment Data

This folder contains a compact subset prepared specifically for the public Streamlit dashboard.

It does **not** contain the raw Apache logs or the full feature dataset.

The files are generated locally with:

```bash
python src/analysis/prepare_dashboard_data.py
```

Do not manually edit the generated Parquet files.
